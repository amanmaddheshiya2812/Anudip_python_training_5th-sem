total_bill = 0
while True:
    price = float(input("Enter Item Price: "))
    # Check for the exit condition
    if price == 0:
        break
    total_bill += price
print(f"\nTotal Bill Amount:{total_bill:.0f}")